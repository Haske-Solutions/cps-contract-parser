# CI/local unsigned macOS arm64 DMG (single-arch; no --arm64 CLI flag needed).
extends: electron-builder.yml
mac:
  hardenedRuntime: false
  gatekeeperAssess: false
  target:
    - target: dmg
      arch:
        - arm64
