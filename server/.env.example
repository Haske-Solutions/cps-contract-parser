# Parser API (required)
PARSER_API_KEY=change-me-to-a-long-random-secret

# AWS Bedrock credentials (IAM user keys or instance profile on AWS VPS)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Server
PORT=8080
